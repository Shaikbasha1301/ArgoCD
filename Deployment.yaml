apiVersion: apps/v1
kind: Deployment
metadata:
  name: mydeploy
spec:
  replicas: 2
  selector:
    matchLabels:
      name: myapp
  template:
    metadata:
      labels:
        name: myapp  
    spec:
      containers:
      - name: mycont
        image: shaikmustafa/mygame
        ports:
        - containerPort: 80
